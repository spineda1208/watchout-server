# Database connection (must match your Next.js app's database)
# This is used for session verification (if using direct database query method)
DATABASE_URL=postgresql://user:password@localhost:5432/watchout

# Server configuration
PORT=3000

# Next.js API URL for session verification
# Used when verifying tokens via API call (default method)
# Should point to your Next.js app
NEXT_PUBLIC_API_URL=http://localhost:3001

# Better Auth configuration
# Note: Session cookie name and other settings should match your Next.js app
# Default cookie name: better-auth.session_token
# Update src/auth.ts if you use custom settings
