# Database connection (REQUIRED)
# IMPORTANT: This MUST be the same database URL as your Next.js app (watchout-web)
# The WebSocket server needs to access the same database to verify Better Auth sessions
# 
# For local development with docker-compose in watchout-web:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/watchout
#
# For Neon or other hosted database, copy the same URL from watchout-web
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/watchout

# Server configuration
PORT=3000

# Authentication strategy (choose one or more)
# 1) JWT verification (preferred for mobile-app issued tokens)
#    - Use JWT_SECRET for HMAC-signed tokens (HS256/HS512, etc)
#    - Or JWT_PUBLIC_KEY for asymmetric tokens (paste PEM/SPKI). Set JWT_ISSUER/AUDIENCE as needed.
#    - Or JWT_JWKS_URL to fetch keys remotely.
#JWT_SECRET=your_hmac_secret_value
#JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
#JWT_JWKS_URL=https://your-domain/.well-known/jwks.json
#JWT_ISSUER=your-issuer
#JWT_AUDIENCE=your-audience

# 2) Introspection endpoint (your Next.js API validates provider tokens and returns claims)
#INTROSPECTION_URL=http://localhost:3001/api/auth/introspect
#INTROSPECTION_METHOD=POST
#INTROSPECTION_TOKEN_PARAM=token
# Optional custom auth header for the server-to-server call
#INTROSPECTION_AUTH_HEADER_NAME=Authorization
#INTROSPECTION_AUTH_HEADER_VALUE=Bearer your-internal-secret

# Next.js Web App URL (OPTIONAL - for CORS or webhooks)
# If you need to make API calls back to the Next.js app
NEXTJS_URL=http://localhost:3001
